version: ruleoak.manifest.v1
project:
  name: conformance-agent
agent:
  name: fixture-agent
permissions:
  allowed:
    - search_docs
  approvalRequired:
    - send_email
  blocked:
    - delete_file
evidence:
  format: jsonl
  required: true
