# RuleOak Agentic Conformance Kit

License: **MIT** for fixtures and schema examples.

Use this kit when another agent project wants to emit RuleOak-compatible evidence without embedding the RuleOak runtime.

Run from repo root:

```bash
npm run agentic:conformance
npm run agentic:conformance:json
```
