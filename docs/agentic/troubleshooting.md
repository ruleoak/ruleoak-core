# Troubleshooting

If an action is denied, inspect the policy decision event in Evidence JSONL and verify `.ruleoak.yml` permissions. If replay is empty, check that the Flight Recorder sink path is writable.
