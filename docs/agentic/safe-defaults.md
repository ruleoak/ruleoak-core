# Safe Defaults

Unknown dangerous actions should fail closed. Write, delete, shell, send, spend, deploy, production, and credential actions require deny, dry-run, or approval unless a project policy explicitly allows them.
