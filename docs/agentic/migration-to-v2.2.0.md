# Migration to v2.2.0

Migrate developer-preview agentic features to stable names under `@ruleoak/core/agentic`. Use Evidence JSONL v1 and `.ruleoak.yml` v1. Replace internal preview file imports with the public entry point.
