# Design note

The integration normalizes high-risk actions into a portable action envelope before execution. External tools can evaluate the envelope, require approval, record evidence, or deny execution.
