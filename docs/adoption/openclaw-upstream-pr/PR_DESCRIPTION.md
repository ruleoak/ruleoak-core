# Optional RuleOak-compatible permission/evidence hook

This proposes an optional integration pattern for action permission review and replayable evidence. It does not require OpenClaw-style projects to adopt RuleOak Core as a hard dependency.

The integration can operate in three modes:

1. protocol-only Evidence JSONL emission
2. optional RuleOak runtime adapter
3. documentation-only safety pattern
