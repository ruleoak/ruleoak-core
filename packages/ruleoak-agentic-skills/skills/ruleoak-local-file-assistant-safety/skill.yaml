version: ruleoak.skill.v1
name: ruleoak-local-file-assistant-safety
description: Governed reference skill for ruleoak-local-file-assistant-safety.
ruleoak:
  evidenceFormat: ruleoak.agentic.evidence.v1
  approvalRequired: true
  manifest:
    version: ruleoak.manifest.v1
    project:
      name: ruleoak-local-file-assistant-safety
    agent:
      name: ruleoak-local-file-assistant-safety
      runtime: skill
    permissions:
      allowedActions:
        - search.read
      approvalRequired:
        - email.send
      blockedActions:
        - filesystem.delete
        - shell.execute
    evidence:
      enabled: true
      format: jsonl
      replayable: true
    redaction:
      enabled: true
