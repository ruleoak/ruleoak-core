# Public Agentic Demo

Run:

```bash
npm run agentic:public-demo
```

This is a safe, offline, dramatic demo for README, website, and launch materials. It shows RuleOak governing actions that look like email, shell, secret access, and file deletion without touching real systems.
