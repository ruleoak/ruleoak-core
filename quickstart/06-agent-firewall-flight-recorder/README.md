# Quickstart 06 — Agent Firewall + Flight Recorder

Run:

```bash
npm run agentic:quickstart
```

This quickstart writes evidence to `quickstart/out/agentic-evidence.jsonl` and a replay timeline to `quickstart/out/agentic-replay.md`.
